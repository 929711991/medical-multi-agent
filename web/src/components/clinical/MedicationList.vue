<script setup lang="ts">import type { Medication } from '../../types/patient'; defineProps<{ items: Medication[] }>()</script>
<template><div v-if="items.length" class="medications"><div v-for="item in items" :key="item.id"><span class="pill">Rx</span><div><strong>{{ item.name }}</strong><p>{{ item.dose || '剂量未记录' }} · {{ item.route || '途径未记录' }}</p></div><span :class="item.ended_at ? 'ended' : 'current'">{{ item.ended_at ? '已停用' : '使用中' }}</span></div></div><p v-else class="empty-copy">暂无用药记录</p></template>
<style scoped lang="scss">.medications > div { display: flex; align-items: center; gap: 12px; padding: 14px 4px; border-bottom: 1px solid var(--border); }.pill { width: 32px; height: 32px; display: grid; place-items: center; border-radius: 9px; background: #e8f0ff; color: var(--primary); font-size: 11px; font-weight: 800; }.medications div div { flex: 1; }.medications strong { font-size: 13px; }.medications p { margin: 4px 0 0; color: var(--text-secondary); font-size: 11px; }.current, .ended { font-size: 11px; }.current { color: var(--risk-low); }.ended { color: var(--text-tertiary); }</style>
