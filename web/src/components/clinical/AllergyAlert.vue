<script setup lang="ts">import type { Allergy } from '../../types/patient'; defineProps<{ items: Allergy[] }>()</script>
<template><article class="alert-card" :class="{ safe: !items.length }"><header><h3>药物过敏</h3><span>{{ items.length ? '需要关注' : '无记录' }}</span></header><template v-if="items.length"><div v-for="item in items" :key="item.id" class="allergy"><strong>{{ item.substance }}</strong><p>{{ item.reaction || '未记录反应' }} · {{ item.severity || '未分级' }}</p></div></template><p v-else class="none">暂无已记录药物过敏</p></article></template>
<style scoped lang="scss">.alert-card { padding: 18px; border-radius: var(--radius); background: #fff7ed; border-left: 3px solid var(--risk-high); }.alert-card.safe { background: #f0fdf4; border-color: var(--risk-low); }.alert-card header { display: flex; justify-content: space-between; }.alert-card h3 { margin: 0; font-size: 14px; }.alert-card header span { font-size: 11px; color: var(--risk-high); }.allergy { margin-top: 14px; }.allergy strong { color: #9a3412; }.allergy p, .none { margin: 4px 0 0; font-size: 12px; color: var(--text-secondary); }</style>
