<script setup lang="ts">import type { Visit } from '../../types/patient'; import { formatDateTime } from '../../utils/format'; defineProps<{ visits: Visit[] }>()</script>
<template><article class="timeline-card"><header><h3>临床时间线</h3><span>最近 {{ visits.length }} 次</span></header><div v-if="visits.length" class="timeline"><div v-for="item in visits" :key="item.id"><i /><time>{{ formatDateTime(item.visit_time) }}</time><strong>{{ item.department }}</strong><p>{{ item.chief_complaint }}</p></div></div><p v-else class="empty">暂无近期就诊记录</p></article></template>
<style scoped lang="scss">.timeline-card { padding: 20px; background: white; border-radius: var(--radius); box-shadow: var(--shadow); }.timeline-card header { display: flex; justify-content: space-between; }.timeline-card h3 { margin: 0; font-size: 15px; }.timeline-card header span, time { color: var(--text-tertiary); font-size: 11px; }.timeline { margin-top: 16px; }.timeline > div { position: relative; padding: 0 0 20px 20px; border-left: 1px solid var(--border); display: grid; gap: 4px; }.timeline i { position: absolute; width: 9px; height: 9px; border-radius: 50%; background: var(--secondary); left: -5px; top: 2px; box-shadow: 0 0 0 4px #e8fffb; }.timeline strong { font-size: 13px; }.timeline p, .empty { margin: 0; color: var(--text-secondary); font-size: 12px; line-height: 1.5; }</style>
