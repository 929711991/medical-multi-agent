<script setup lang="ts">import { Icon } from '@iconify/vue'; import type { RiskLevel } from '../../types/diagnosis'; import { riskLabels } from '../../utils/medical'; defineProps<{ risk: RiskLevel }>()</script>
<template><div class="risk-banner" :class="risk"><Icon :icon="risk === 'emergency' ? 'solar:siren-rounded-linear' : 'solar:danger-triangle-linear'" /><div><strong>{{ risk === 'emergency' ? '存在紧急风险征象' : `${riskLabels[risk]}提示` }}</strong><p v-if="risk === 'emergency'">检测到可能需要优先处理的临床风险，请优先进行临床评估，不应等待 AI 流程完成。</p><p v-else>请结合患者当前状态完成临床评估，AI 风险分级仅用于辅助提醒。</p></div></div></template>
<style scoped lang="scss">.risk-banner { display: flex; gap: 12px; padding: 14px 18px; border-radius: 10px; background: #f0fdf4; color: #166534; border-left: 4px solid var(--risk-low); }.risk-banner > svg { font-size: 23px; flex: 0 0 auto; }.risk-banner strong { font-size: 14px; }.risk-banner p { margin: 4px 0 0; font-size: 12px; line-height: 1.5; }.medium { background: #fffbeb; color: #92400e; border-color: var(--risk-medium); }.high { background: #fff7ed; color: #9a3412; border-color: var(--risk-high); }.emergency { background: #fff1f2; color: #991b1b; border-color: var(--risk-emergency); }</style>
