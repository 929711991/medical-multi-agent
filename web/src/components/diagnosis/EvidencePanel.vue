<script setup lang="ts">import { Icon } from '@iconify/vue'; import type { Evidence } from '../../types/diagnosis'; defineProps<{ enabled: boolean; evidence: Evidence[] }>()</script>
<template><section><div v-if="!enabled" class="rag-disabled"><Icon icon="solar:book-bookmark-linear" /><div><strong>当前未启用外部医学知识库</strong><p>本次分析未引用外部 RAG 证据，不展示任何伪造指南或来源。</p></div></div><div v-else-if="evidence.length" class="evidence-list"><article v-for="item in evidence" :key="item.chunk_id"><strong>{{ item.title }}</strong><p>{{ item.excerpt }}</p><span>{{ item.source_type }} · {{ new Date(item.retrieved_at).toLocaleDateString('zh-CN') }}</span></article></div><p v-else class="empty-copy">本次分析没有检索到可用外部证据</p></section></template>
<style scoped lang="scss">.rag-disabled { display: flex; gap: 10px; padding: 14px; border-radius: 9px; background: #f8fafc; color: var(--text-secondary); }.rag-disabled svg { font-size: 21px; }.rag-disabled strong { color: var(--text-primary); font-size: 12px; }.rag-disabled p { margin: 4px 0 0; font-size: 11px; line-height: 1.5; }.evidence-list { display: grid; gap: 10px; }.evidence-list article { padding: 13px; border: 1px solid var(--border); border-radius: 8px; }.evidence-list p { color: var(--text-secondary); font-size: 12px; line-height: 1.55; }.evidence-list span { color: var(--text-tertiary); font-size: 10px; }</style>
