<script setup lang="ts">import type { PossibleCondition } from '../../types/diagnosis'; import { confidenceLabel } from '../../utils/medical'; defineProps<{ condition: PossibleCondition; index?: number }>()</script>
<template><article class="condition"><div class="rank">{{ (index ?? 0) + 1 }}</div><div><header><strong>{{ condition.name }}</strong><span>{{ confidenceLabel(condition.confidence) }}</span></header><p>{{ condition.reason }}</p></div></article></template>
<style scoped lang="scss">.condition { display: grid; grid-template-columns: 30px 1fr; gap: 12px; padding: 15px 0; border-bottom: 1px solid var(--border); }.condition:last-child { border: 0; }.rank { width: 28px; height: 28px; border-radius: 8px; background: #edf4ff; color: var(--primary); display: grid; place-items: center; font-size: 12px; font-weight: 800; }.condition header { display: flex; justify-content: space-between; gap: 12px; }.condition header span { color: var(--secondary); background: #eafbf8; border-radius: 99px; padding: 3px 8px; font-size: 11px; white-space: nowrap; }.condition p { margin: 6px 0 0; color: var(--text-secondary); font-size: 12px; line-height: 1.55; }</style>
