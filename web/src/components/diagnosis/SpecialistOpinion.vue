<script setup lang="ts">import type { SpecialistOpinion } from '../../types/diagnosis'; import { specialtyLabel } from '../../utils/medical'; defineProps<{ opinion: SpecialistOpinion }>()</script>
<template><article class="opinion"><header><span class="specialty-mark">{{ opinion.specialty === 'cardiology' ? '心' : '消' }}</span><div><strong>{{ specialtyLabel(opinion.specialty) }}专业分析</strong><small>专科智能体辅助意见</small></div></header><p>{{ opinion.summary }}</p><div v-if="opinion.key_findings.length" class="findings"><span v-for="item in opinion.key_findings" :key="item">{{ item }}</span></div></article></template>
<style scoped lang="scss">.opinion { padding: 16px; border-radius: 10px; background: #f7fafc; border: 1px solid var(--border); }.opinion header { display: flex; gap: 10px; align-items: center; }.specialty-mark { width: 34px; height: 34px; display: grid; place-items: center; border-radius: 9px; color: white; background: var(--secondary); font-weight: 800; }.opinion header div { display: grid; gap: 2px; }.opinion small { color: var(--text-tertiary); }.opinion > p { color: var(--text-secondary); font-size: 13px; line-height: 1.6; }.findings { display: grid; gap: 5px; }.findings span { font-size: 12px; }.findings span::before { content: '·'; color: var(--secondary); font-weight: 900; margin-right: 7px; }</style>
