<script setup lang="ts">import AppPage from '../../components/common/AppPage.vue'; import { useAuthStore } from '../../stores/auth'; const auth = useAuthStore()</script><template><AppPage title="个人中心" description="当前登录医生与工作环境"><section class="surface profile"><div class="avatar">{{ auth.user?.name.slice(-2) }}</div><div><h2>{{ auth.user?.name }}</h2><p>{{ auth.user?.department }} · {{ auth.user?.title }}</p><span class="mono">{{ auth.user?.doctor_id }}</span></div></section><section class="surface boundary"><h3>临床安全边界</h3><p>AI 生成内容仅作为医生辅助决策草稿，必须由登录医生完成通过、修改或驳回。系统不会从浏览器接收或信任审核医生编号。</p></section></AppPage></template><style scoped lang="scss">.profile { display: flex; gap: 16px; align-items: center; padding: 24px; }.avatar { width: 58px; height: 58px; display: grid; place-items: center; border-radius: 14px; background: #edf4ff; color: var(--primary); font-weight: 800; }.profile h2 { margin: 0; }.profile p { color: var(--text-secondary); margin: 7px 0; }.profile span { color: var(--text-tertiary); font-size: 11px; }.boundary { margin-top: 16px; padding: 22px; }.boundary h3 { margin: 0; font-size: 15px; }.boundary p { color: var(--text-secondary); line-height: 1.7; font-size: 13px; max-width: 760px; }</style>
